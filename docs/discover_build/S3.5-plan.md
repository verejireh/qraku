# S3.5 — 동적 주방 대기시간 + Order↔Store 통일 (스펙 + 계획)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.
> 브랜치: `discover_build` 계열 · 작성일: 2026-06-09 · 상위: [WORKPLAN.md](./WORKPLAN.md)

**Goal:** S3에서 노출한 **정적** 픽업 ETA(`takeout_default_wait_minutes`)를, 지금 매장에 밀려 있는 **미완료 테이크아웃 주문 적체**를 반영한 **동적 대기시간**으로 확장한다. 동시에, 이 작업이 의존하는 주문↔매장 집계의 **기존 버그**(`Order.store_id` 참조 — 존재하지 않는 컬럼)를 **단일 진실 헬퍼**로 통일해 영구 차단한다.

**확정된 설계 결정 (브레인스토밍 2026-06-09):**
1. **통일 방식**: 공유 헬퍼로 `Order.shop_id` 기준 통일 (마이그레이션 없음). `Order`엔 `store_id`가 없고 `shop_id: str`에 slug 또는 str(id)가 polymorphic하게 저장됨 — 매장당 `{slug, str(id)}` 후보 집합으로 매칭하는 [orders.py:964](../../backend/routers/orders.py:964)의 검증된 패턴을 헬퍼로 추출.
2. **동적 산식**: `dynamic = base + backlog × 3분`, 상한 **60분**. `base = takeout_default_wait_minutes`(업주 설정), `backlog = 미완료 테이크아웃 주문 수`(실시간). 건당 3분·상한 60분은 **플랫폼 고정 상수**(업주 비노출).
3. **캐싱**: Redis **60초 TTL**. Redis 실패 시 직접 계산 폴백 → `/nearby` 절대 안 깨짐.
4. **admin 안내**: 동적 대기가 어떻게 작동하는지 **읽기 전용 설명 카드**를 admin 운영관리(테이크아웃 섹션)에 추가. ③·④는 비노출, 업주는 ① base만 조절.

---

## 산식 정의

```
backlog(매장) = COUNT(order
                WHERE shop_id ∈ {slug, str(id)}
                  AND order_type = 'take_out'
                  AND payment_status = 'paid'
                  AND needs_serving = TRUE)

dynamic_wait = min(base + backlog × MINUTES_PER_ORDER, WAIT_CAP_MINUTES)
  base               = Store.takeout_default_wait_minutes (기본 15)
  MINUTES_PER_ORDER  = 3   (플랫폼 상수)
  WAIT_CAP_MINUTES   = 60  (플랫폼 상수)
```

- **결제 완료(`paid`)만**: 미결제 주문은 주방이 안 만들므로 부하 아님.
- **미서빙(`needs_serving=TRUE`)만**: 이미 픽업된 주문은 일감 종료.
- **테이크아웃만**: 사용자 확정 기준(eat-in 제외).

---

## File Fence (변경 허용 파일)

| 파일 | 변경 |
|---|---|
| `backend/utils/order_store.py` | **신규** — shop_id 후보 헬퍼 (단일 진실) |
| `backend/utils/takeout_wait.py` | **신규** — 동적 대기 집계 + Redis 캐싱 |
| `backend/utils/nearby.py` | `takeout_dynamic_wait_minutes` 필드 추가 (기존 키 불변) |
| `backend/routers/discover.py` | `_get_store_order_stats`·메뉴 집계 버그 수정(헬퍼 사용) + `/nearby` 응답에 상수 에코 |
| `frontend-react/src/views/DiscoverView.jsx` | 동적값 우선 표시 |
| `frontend-react/src/components/DiscoverNearbyMap.jsx` | 동적값 우선 표시 |
| `frontend-react/src/views/AdminOperationView.jsx` | 동적 대기 설명 카드 (읽기 전용) |
| `backend/tests/test_order_store.py` | **신규** — 헬퍼 단위테스트 |
| `backend/tests/test_takeout_wait.py` | **신규** — 산식(순수함수) 단위테스트 |

**불가침:** 결제 로직, `models.py`(컬럼 추가 없음 — 기존 필드만 SELECT), 마이그레이션, `/nearby`의 기존 응답 키, `MagnoliaCartModal.jsx`, `OrderView.jsx` props.

**범위 밖(기록만):** [loyalty_analytics.py:42](../../backend/routers/loyalty_analytics.py:42)도 동일하게 `Order.store_id`(+ 없는 `Order.final_price`) 참조 → 별도 도메인이라 이번 미수정. 후속 과제로 분리.

---

## Task 1 — `utils/order_store.py` (신규, 순수 헬퍼)

**File:** `backend/utils/order_store.py` (신규)

- [ ] **Step 1:** 아래 모듈 작성. `(store_id, slug)` 튜플 기반(Store 객체·SQL row 양쪽에서 사용 가능):
```python
"""Order ↔ Store 연결의 단일 진실 공급원.

Order.shop_id 에는 주문 생성 시점의 slug 또는 str(id) 가 그대로 저장된다(polymorphic,
[orders.py] _resolve_store_by_shop_id 참고). 따라서 매장↔주문 집계는 반드시 이 헬퍼로
매장당 후보 집합 {slug, str(id)} 을 만들어 Order.shop_id 와 매칭한다.

금지: Order.store_id (존재하지 않는 컬럼) 또는 Order.shop_id == <int> 직접 비교.
      → 이것이 discover 집계 버그의 재발 원인이었다.
"""
from typing import Iterable, Optional, Tuple

StoreKey = Tuple[int, Optional[str]]  # (store_id, slug)


def store_shop_id_candidates(store_id: int, slug: Optional[str]) -> set[str]:
    """한 매장의 가능한 Order.shop_id 값 전체."""
    cands = {str(store_id)}
    if slug:
        cands.add(slug)
    return cands


def all_shop_id_candidates(stores: Iterable[StoreKey]) -> list[str]:
    """여러 매장의 모든 후보 shop_id 문자열(중복 제거). WHERE shop_id IN (...) 용."""
    out: set[str] = set()
    for store_id, slug in stores:
        out |= store_shop_id_candidates(store_id, slug)
    return list(out)


def shop_id_to_store_id(stores: Iterable[StoreKey]) -> dict[str, int]:
    """후보 shop_id 문자열 → 정규 Store.id 역매핑. 집계 결과를 매장으로 되돌릴 때."""
    m: dict[str, int] = {}
    for store_id, slug in stores:
        for c in store_shop_id_candidates(store_id, slug):
            m[c] = store_id
    return m
```
- [ ] **Step 2:** 구문 검증 `uv run python -c "import ast; ast.parse(open('backend/utils/order_store.py',encoding='utf-8').read()); print('OK')"`
- [ ] **Step 3:** 커밋 `git add backend/utils/order_store.py && git commit -m "feat(discover): Order↔Store shop_id 후보 헬퍼 (단일 진실) (S3.5 T1)"`

---

## Task 2 — `test_order_store.py` (신규 단위테스트)

**File:** `backend/tests/test_order_store.py` (신규) — `test_takeout.py` 스타일(순수함수, fixture 없음)

- [ ] **Step 1:** 테스트 작성:
```python
from utils.order_store import (
    store_shop_id_candidates, all_shop_id_candidates, shop_id_to_store_id,
)


def test_candidates_include_id_and_slug():
    assert store_shop_id_candidates(7, "ramen-ya") == {"7", "ramen-ya"}


def test_candidates_id_only_when_no_slug():
    assert store_shop_id_candidates(7, None) == {"7"}


def test_all_candidates_dedup():
    got = set(all_shop_id_candidates([(1, "a"), (2, None)]))
    assert got == {"1", "a", "2"}


def test_reverse_map_each_candidate_to_store():
    m = shop_id_to_store_id([(1, "a"), (2, "b")])
    assert m == {"1": 1, "a": 1, "2": 2, "b": 2}
```
- [ ] **Step 2:** 실행 `uv run --with pytest pytest backend/tests/test_order_store.py -q` → 4 passed
- [ ] **Step 3:** 커밋 `git add backend/tests/test_order_store.py && git commit -m "test(discover): order_store 헬퍼 단위테스트 (S3.5 T2)"`

---

## Task 3 — `discover.py` 집계 버그 수정 (헬퍼 사용)

**File:** `backend/routers/discover.py`

> 현재 `_get_store_order_stats`(L27)와 메뉴 집계(L115-122)는 **존재하지 않는 `Order.store_id`**를 참조 → 런타임 깨짐. `Table.store_id`는 실제 FK이므로 그대로 둔다.

- [ ] **Step 1:** import 추가 (상단 import 블록):
```python
from utils.order_store import all_shop_id_candidates, shop_id_to_store_id
```
- [ ] **Step 2:** `_get_store_order_stats` 시그니처를 `store_ids: List[int]` → `stores: List[Store]`(또는 `(id,slug)` 키)로 변경하고 본문의 Order 집계를 후보/역매핑 방식으로 교체:
```python
async def _get_store_order_stats(session: AsyncSession, stores: List[Store], days: int = 30) -> Dict[int, Dict]:
    """최근 N일간 가게별 주문 통계 (총 주문수, 테이블 수). Order.shop_id(polymorphic) 기준."""
    if not stores:
        return {}

    keys = [(s.id, s.slug) for s in stores]
    store_ids = [s.id for s in stores]
    candidates = all_shop_id_candidates(keys)
    rev = shop_id_to_store_id(keys)

    since = now_utc_naive() - timedelta(days=days)

    # 최근 N일 주문수 — Order.shop_id 후보로 매칭 후 store.id 로 환원
    order_result = await session.execute(
        select(Order.shop_id, func.count(Order.id).label("order_count"))
        .where(Order.shop_id.in_(candidates))
        .where(Order.created_at >= since)
        .group_by(Order.shop_id)
    )
    order_counts: Dict[int, int] = {}
    for row in order_result:
        sid = rev.get(row.shop_id)
        if sid is not None:
            order_counts[sid] = order_counts.get(sid, 0) + row.order_count

    # 테이블 수 (Table.store_id 는 실제 FK — 그대로)
    table_result = await session.execute(
        select(Table.store_id, func.count(Table.id).label("table_count"))
        .where(Table.store_id.in_(store_ids))
        .group_by(Table.store_id)
    )
    table_counts = {row.store_id: row.table_count for row in table_result}

    stats = {}
    for sid in store_ids:
        orders = order_counts.get(sid, 0)
        tables = max(table_counts.get(sid, 1), 1)
        stats[sid] = {
            "order_count": orders,
            "table_count": tables,
            "orders_per_table": round(orders / tables, 2),
        }
    return stats
```
- [ ] **Step 3:** 호출부 2곳을 `stores` 전달로 변경:
  - `discover_menus`: `stats = await _get_store_order_stats(session, stores)` (이미 `stores` 보유, `store_ids` 인자 제거)
  - `discover_stores`: `stats = await _get_store_order_stats(session, stores)`
- [ ] **Step 4:** 메뉴별 주문 집계(L115-122) `Order.store_id.in_(store_ids)` → 후보 매칭:
```python
    candidates = all_shop_id_candidates([(s.id, s.slug) for s in stores])
    item_order_result = await session.execute(
        select(OrderItem.menu_id, func.sum(OrderItem.quantity).label("qty"))
        .join(Order, Order.id == OrderItem.order_id)
        .where(Order.shop_id.in_(candidates))
        .where(Order.created_at >= since)
        .group_by(OrderItem.menu_id)
    )
```
- [ ] **Step 5:** 구문 검증 `uv run python -c "import ast; ast.parse(open('backend/routers/discover.py',encoding='utf-8').read()); print('OK')"`
- [ ] **Step 6:** 커밋 `git add backend/routers/discover.py && git commit -m "fix(discover): 주문 집계를 Order.shop_id 후보 매칭으로 수정 (store_id 미존재 버그) (S3.5 T3)"`

---

## Task 4 — `utils/takeout_wait.py` (신규, 동적 대기 + Redis 캐싱)

**File:** `backend/utils/takeout_wait.py` (신규)

- [ ] **Step 1:** 모듈 작성. 순수 산식 함수 + DB/Redis 집계 분리, Redis 실패 폴백:
```python
"""테이크아웃 동적 픽업 대기시간 — 미완료 테이크아웃 적체를 분으로 환산.

dynamic = min(base + backlog × MINUTES_PER_ORDER, WAIT_CAP_MINUTES)
Redis 60초 TTL 로 매장별 backlog 캐싱. Redis 불가 시 직접 계산 폴백 — /nearby 절대 안 깨짐.
"""
import logging
from typing import Iterable, Optional, Tuple

from sqlmodel import select, func
from sqlalchemy.ext.asyncio import AsyncSession

from models import Order
from utils.order_store import all_shop_id_candidates, shop_id_to_store_id

logger = logging.getLogger(__name__)

MINUTES_PER_ORDER = 3
WAIT_CAP_MINUTES = 60
CACHE_TTL_SECONDS = 60
_CACHE_PREFIX = "discover:wait:backlog:"  # +{store_id} -> backlog count (str)

# (store_id, slug, base_wait_minutes)
StoreWaitKey = Tuple[int, Optional[str], Optional[int]]


def dynamic_wait_minutes(base: Optional[int], backlog: int) -> int:
    """순수 산식. base 가 없으면 15 로 간주."""
    b = base if base and base > 0 else 15
    return min(b + backlog * MINUTES_PER_ORDER, WAIT_CAP_MINUTES)


async def _query_backlog(session: AsyncSession, candidates: list[str], rev: dict[str, int]) -> dict[int, int]:
    """단일 GROUP BY 쿼리로 매장별 미완료 테이크아웃 주문 수 (ORM — 예약어/배열바인딩 회피)."""
    if not candidates:
        return {}
    result = await session.execute(
        select(Order.shop_id, func.count(Order.id).label("cnt"))
        .where(Order.shop_id.in_(candidates))
        .where(Order.order_type == "take_out")
        .where(Order.payment_status == "paid")
        .where(Order.needs_serving == True)  # noqa: E712 (SQLAlchemy 비교)
        .group_by(Order.shop_id)
    )
    backlog: dict[int, int] = {}
    for row in result:
        sid = rev.get(row.shop_id)
        if sid is not None:
            backlog[sid] = backlog.get(sid, 0) + int(row.cnt)
    return backlog


async def compute_dynamic_waits(session: AsyncSession, stores: Iterable[StoreWaitKey]) -> dict[int, int]:
    """{store_id: dynamic_wait_minutes}. Redis 60s 캐시 → 미스만 단일 쿼리 집계."""
    stores = list(stores)
    if not stores:
        return {}
    keys = [(sid, slug) for sid, slug, _ in stores]
    base_by_id = {sid: base for sid, _, base in stores}
    all_ids = [sid for sid, _, _ in stores]

    backlog: dict[int, int] = {}
    miss_ids: list[int] = list(all_ids)

    # 1) Redis 캐시 조회 (실패 시 전체 미스 처리)
    redis = None
    try:
        from utils.redis import get_redis
        redis = get_redis()
        cached = await redis.mget([f"{_CACHE_PREFIX}{sid}" for sid in all_ids])
        miss_ids = []
        for sid, val in zip(all_ids, cached):
            if val is not None:
                backlog[sid] = int(val)
            else:
                miss_ids.append(sid)
    except Exception:
        logger.debug("takeout_wait: Redis 캐시 미사용 (폴백)", exc_info=True)
        redis = None
        miss_ids = list(all_ids)

    # 2) 캐시 미스 매장만 단일 쿼리 집계
    if miss_ids:
        miss_keys = [(sid, dict(keys)[sid]) for sid in miss_ids]
        candidates = all_shop_id_candidates(miss_keys)
        rev = shop_id_to_store_id(miss_keys)
        fresh = await _query_backlog(session, candidates, rev)
        for sid in miss_ids:
            backlog[sid] = fresh.get(sid, 0)
        # 3) 캐시 기록 (실패 무시)
        if redis is not None:
            try:
                pipe = redis.pipeline()
                for sid in miss_ids:
                    pipe.set(f"{_CACHE_PREFIX}{sid}", str(backlog[sid]), ex=CACHE_TTL_SECONDS)
                await pipe.execute()
            except Exception:
                logger.debug("takeout_wait: Redis 캐시 기록 실패 (무시)", exc_info=True)

    return {sid: dynamic_wait_minutes(base_by_id.get(sid), backlog.get(sid, 0)) for sid in all_ids}
```
> `dict(keys)[sid]`는 slug 조회용. `keys`는 `(id, slug)` 리스트라 `dict(keys)`로 `{id: slug}` 매핑.
- [ ] **Step 2:** 구문 검증 `uv run python -c "import ast; ast.parse(open('backend/utils/takeout_wait.py',encoding='utf-8').read()); print('OK')"`
- [ ] **Step 3:** 커밋 `git add backend/utils/takeout_wait.py && git commit -m "feat(discover): 테이크아웃 동적 대기시간 집계 + Redis 60s 캐싱 (S3.5 T4)"`

---

## Task 5 — `test_takeout_wait.py` (순수 산식 단위테스트)

**File:** `backend/tests/test_takeout_wait.py` (신규)

- [ ] **Step 1:** 산식 함수 테스트(DB/Redis 불필요):
```python
from utils.takeout_wait import dynamic_wait_minutes, MINUTES_PER_ORDER, WAIT_CAP_MINUTES


def test_no_backlog_returns_base():
    assert dynamic_wait_minutes(15, 0) == 15


def test_backlog_adds_minutes_per_order():
    assert dynamic_wait_minutes(15, 3) == 15 + 3 * MINUTES_PER_ORDER  # 24


def test_cap_applies():
    assert dynamic_wait_minutes(15, 100) == WAIT_CAP_MINUTES


def test_none_base_defaults_to_15():
    assert dynamic_wait_minutes(None, 0) == 15
    assert dynamic_wait_minutes(0, 2) == 15 + 2 * MINUTES_PER_ORDER
```
- [ ] **Step 2:** 실행 `uv run --with pytest pytest backend/tests/test_takeout_wait.py -q` → 4 passed
- [ ] **Step 3:** 커밋 `git add backend/tests/test_takeout_wait.py && git commit -m "test(discover): 동적 대기 산식 단위테스트 (S3.5 T5)"`

---

## Task 6 — `nearby.py`: `takeout_dynamic_wait_minutes` 필드 추가

**File:** `backend/utils/nearby.py` (`find_nearby_stores`)

> 기존 키 전부 유지(R3 `/nearby` 불변). 새 키만 추가 → LINE 카드도 자동 수혜.

- [ ] **Step 1:** import 추가: `from utils.takeout_wait import compute_dynamic_waits`
- [ ] **Step 2:** `items` 빌드 루프 **다음**(`if takeout_only:` 필터 **전**)에 동적 대기 계산·부착:
```python
    # 동적 픽업 대기 (Redis 60s 캐시, 실패 시 직접 계산 폴백)
    wait_keys = [(it["store_id"], it["slug"], it["takeout_default_wait_minutes"]) for it in items]
    try:
        dyn = await compute_dynamic_waits(session, wait_keys)
    except Exception:
        dyn = {}
    for it in items:
        it["takeout_dynamic_wait_minutes"] = dyn.get(it["store_id"])
```
- [ ] **Step 3:** docstring 의 Returns 키 목록 끝에 `takeout_dynamic_wait_minutes` 추가(문서 일관성).
- [ ] **Step 4:** 구문 검증 `uv run python -c "import ast; ast.parse(open('backend/utils/nearby.py',encoding='utf-8').read()); print('OK')"`
- [ ] **Step 5:** 커밋 `git add backend/utils/nearby.py && git commit -m "feat(discover): /nearby 에 takeout_dynamic_wait_minutes 추가 (S3.5 T6)"`

---

## Task 7 — `discover.py` `/nearby` 응답에 상수 에코 (선택, 클라이언트 디버그용)

**File:** `backend/routers/discover.py` (`discover_nearby` 의 `return {...}`)

- [ ] **Step 1:** 응답 envelope 의 `"open_only": open_only,` 다음에 추가(필드는 items 안에 이미 존재하므로 envelope 에코는 메타 정보용):
```python
        "wait_minutes_per_order": 3,
        "wait_cap_minutes": 60,
```
> 상수는 `takeout_wait` 에서 import 해 하드코딩 중복 회피:
> `from utils.takeout_wait import MINUTES_PER_ORDER, WAIT_CAP_MINUTES` 후 값 사용.
- [ ] **Step 2:** 구문 검증 + 커밋 `git commit -m "feat(discover): /nearby 응답에 동적대기 상수 에코 (S3.5 T7)"`

---

## Task 8 — 프론트: 리스트 카드 동적값 우선

**File:** `frontend-react/src/views/DiscoverView.jsx` (`StoreCard`, 현재 픽업 ETA 라인 [L81-84])

- [ ] **Step 1:** 픽업 ETA 표시값을 동적값 우선·정적값 폴백으로 교체. 기존:
```jsx
        {store.can_accept_takeout && store.is_open && store.takeout_default_wait_minutes > 0 && (
          <p className="text-xs text-[#c21e2f] font-bold flex items-center gap-1">
            <MSI name="schedule" className="text-sm" />
            約{store.takeout_default_wait_minutes}分で受取
          </p>
        )}
```
를 다음으로:
```jsx
        {store.can_accept_takeout && store.is_open && (store.takeout_dynamic_wait_minutes ?? store.takeout_default_wait_minutes) > 0 && (
          <p className="text-xs text-[#c21e2f] font-bold flex items-center gap-1">
            <MSI name="schedule" className="text-sm" />
            約{store.takeout_dynamic_wait_minutes ?? store.takeout_default_wait_minutes}分で受取
          </p>
        )}
```
- [ ] **Step 2:** 커밋 `git add frontend-react/src/views/DiscoverView.jsx && git commit -m "feat(discover-ui): 리스트 카드 동적 픽업 ETA 우선 (S3.5 T8)"`

---

## Task 9 — 프론트: 지도 팝업 동적값 우선

**File:** `frontend-react/src/components/DiscoverNearbyMap.jsx` (현재 팝업 ETA [L68-69])

- [ ] **Step 1:** 동일 패턴으로 교체:
```jsx
                  {s.can_accept_takeout && s.is_open && (s.takeout_dynamic_wait_minutes ?? s.takeout_default_wait_minutes) > 0 && (
                    <p style={{ fontSize: 11, color: '#c21e2f', fontWeight: 700, margin: '0 0 6px' }}>🕒 約{s.takeout_dynamic_wait_minutes ?? s.takeout_default_wait_minutes}分で受取</p>
                  )}
```
- [ ] **Step 2:** 커밋 `git add frontend-react/src/components/DiscoverNearbyMap.jsx && git commit -m "feat(discover-ui): 지도 팝업 동적 픽업 ETA 우선 (S3.5 T9)"`

---

## Task 10 — admin: 동적 대기 설명 카드 (읽기 전용)

**File:** `frontend-react/src/views/AdminOperationView.jsx` (테이크아웃 섹션, デフォルト待ち時間 입력 [L274-283] 내부)

- [ ] **Step 1:** デフォルト待ち時間 입력 블록의 설명 `<p>`(L281 `お客様がピックアップ時間を未入力の場合の初期値です`) **다음**에 안내 카드 삽입(같은 `storeData?.takeout_enabled` 블록 안):
```jsx
                                <div className="mt-3 rounded-xl border border-blue-100 bg-blue-50/60 p-3 space-y-1.5">
                                    <div className="flex items-center gap-1.5">
                                        <span className="material-symbols-outlined text-blue-500 text-[18px]">schedule</span>
                                        <p className="text-xs font-bold text-slate-700">混雑時は待ち時間が自動で延びます</p>
                                    </div>
                                    <p className="text-[11px] text-slate-500 leading-relaxed">
                                        お店が忙しいとき（未完了のテイクアウト注文がたまっているとき）、ディスカバリーでお客様に表示されるピックアップ目安が自動的に延長されます。
                                    </p>
                                    <p className="text-[11px] text-slate-500 leading-relaxed">
                                        目安 = 上記のデフォルト待ち時間 ＋ 未完了テイクアウト注文数 × 約3分（最大60分）。設定は不要です。正直な待ち時間でお客様の信頼につながります。
                                    </p>
                                </div>
```
- [ ] **Step 2:** 커밋 `git add frontend-react/src/views/AdminOperationView.jsx && git commit -m "feat(admin-ops): 동적 픽업 대기 설명 카드 (S3.5 T10)"`

---

## Task 11 — Green Gate (완료 보고 전 필수)

- [ ] **Step 1:** `uv run --with pytest pytest backend/tests/ -q` → 기존 + 신규 전부 green (최소 9 기존 + 8 신규)
- [ ] **Step 2:** `npm --prefix frontend-react run build` → 성공
- [ ] **Step 3:** `cd frontend-react && npx eslint src/views/DiscoverView.jsx src/components/DiscoverNearbyMap.jsx src/views/AdminOperationView.jsx` → 신규 에러 0
- [ ] **Step 4:** (DB+Redis 가용 시) 스모크: `/public/discover/nearby?lat=..&lng=..` 응답에 `takeout_dynamic_wait_minutes` 키 존재 + `/public/discover/menus` 200(집계 버그 해소 확인 — 이전엔 500/AttributeError).

---

## 완료 기준 (DoD)

- [ ] `Order.store_id` 참조 제거 — discover 집계가 `Order.shop_id` 후보 매칭으로 동작(eat-in/take-out 모두). `/menus`·`/stores` 주문 통계 정상.
- [ ] `order_store.py` 단일 헬퍼가 유일한 매칭 경로 — 손으로 짠 잘못된 비교 재발 불가.
- [ ] `/nearby`·LINE 카드에 `takeout_dynamic_wait_minutes` 노출. 기존 키 전부 불변.
- [ ] 동적 산식: base + backlog×3, 상한 60. 미완료 테이크아웃(paid·needs_serving)만 집계.
- [ ] Redis 60s 캐싱 동작 + Redis 실패 시 폴백으로 `/nearby` 안 깨짐.
- [ ] 리스트·지도: 동적값 있으면 우선, 없으면 정적값 폴백.
- [ ] admin 운영관리에 동적 대기 설명 카드(읽기 전용) 노출.
- [ ] Green Gate(pytest·build·신규 lint 0) 통과. models.py·마이그레이션 무수정.

## Self-Review
- **스펙 커버리지**: 버그수정(T1·T3)·헬퍼(T1·T2)·동적집계(T4·T5)·캐싱(T4)·`/nearby` 노출(T6)·프론트(T8·T9)·admin 설명(T10) 매핑 ✅
- **필드 일관성**: 신규 `takeout_dynamic_wait_minutes`만 추가, 기존 `takeout_default_wait_minutes`·`is_open`·`can_accept_takeout` 불변. 프론트는 `?? 폴백`으로 구버전 응답에도 안전.
- **비용**: 캐시 미스 시 매장당 1쿼리 아님 — **단일 GROUP BY 1쿼리** + Redis 60s. `/nearby`는 S2에서 자동검색 없음(저빈도).
- **재발 방지**: 잘못된 `Order.store_id`/`shop_id==int` 비교가 헬퍼로 일원화됨. 모듈 docstring에 금지 규칙 명시.
